# X-Ray model path (GGUF format)
XRAY_MODEL_PATH=
# Context window size (default: 8192)
XRAY_N_CTX=8192
# GPU layers (-1 = all, 0 = CPU only)
XRAY_N_GPU_LAYERS=-1
# Temperature (lower = more deterministic)
XRAY_TEMPERATURE=0.3
