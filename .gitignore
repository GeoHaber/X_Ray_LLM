__pycache__/
*.pyc
*.pyo
venv/
.venv/
.env
.DS_Store
.vscode/
.idea/

# Build artifacts
*.pyd
target/
*.egg-info/
dist/
build/

# Temporary scan results (regeneratable)
*.json
!mothership.json
!requirements.txt

# Old / archived (everything moved here)
_OLD/

# PyInstaller build outputs (regenerated by spec files)
X_Ray_Standalone/
X_Ray_Desktop/
build_exe/
build_web/
build_desktop/

# Rust build cache
Cargo.lock

# Transpiler verification / training artifacts (regeneratable)
_verify_crate/
_training_ground/
scan_output*.txt
verify_output*.txt

# Temporary output / log files
*_output*.txt
full_report.txt
lint_report.txt
x_ray_crash.log
xray_scan_output.txt

# Scratch directory outputs (keep .py test files but ignore outputs)
_scratch/*.txt
_scratch/*.html
_scratch/*.log
_scratch/*.py

# DLLs copied next to the Rust executable (regeneratable)
*.dll

# Rust executable build artifacts at project root
x_ray_rustified.exe
*.exe

# Debug / scan output text files at project root
xray_*.txt
xray_*.json
*_result.txt
*_results.txt
crits_result.txt
failures.txt
pytest_*.txt
rustify_build_err.txt

# One-off analysis / helper scripts at project root (not part of package)
find_crits.py
_check_grade.py
_read_report.py
run_nexus_on_transpiler.py

# Bandit reports
bandit_report*.json
