[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "xray_native"
version = "1.0.0"
description = "X-Ray LLM Rust scanner -- PyO3 native extension"
requires-python = ">=3.9"

[tool.maturin]
features = ["python"]
module-name = "xray_native"
